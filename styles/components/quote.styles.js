const quoteStyles = {
  section: [
    'place-self-center',
    // 'lg:pb-10',
    'lg:w-3/6',
    // 'bg-blue-500'
  ].join(' '),

  h2: ['lg:text-2xl'].join(' '),

  h1: ['lg:text-lg'].join(' '),
};

export default quoteStyles;
